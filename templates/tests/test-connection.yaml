apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kong-collector-chart.fullname" . }}-test-connection"
  labels:
{{ include "kong-collector-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kong-collector-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
